import { createGlobalStyle } from 'styled-components';

export const GlobalStyleContainer = createGlobalStyle`
    .svg-icon {
        width: 20px;
        height: 20px;

        g {
            fill: white;
        }
    }
`